{
  "name": "amazon-keyword-tracker-zeabur-feishu",
  "version": "3.1.0",
  "description": "Amazon ASIN keyword organic/ad rank tracker with progress UI, Excel export, Feishu Bitable write, Zeabur Docker deployment, and optional Sorftime MCP enrichment.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "run": "node src/server.js --once",
    "install-browser": "npx playwright install chromium",
    "doctor": "node -e \"import('./src/browser.js').then(m=>console.log(m.browserStatus()))\""
  },
  "dependencies": {
    "express": "4.21.2",
    "multer": "2.0.2",
    "node-cron": "3.0.3",
    "playwright": "1.60.0",
    "xlsx": "0.18.5"
  },
  "engines": {
    "node": ">=20"
  }
}